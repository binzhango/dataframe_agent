# Deployment

This directory contains deployment configurations for the LLM-Driven Secure Python Execution Platform.

## Structure

- `kubernetes/` - Kubernetes manifests for deploying services
- `docker/` - Dockerfiles for building container images
- `helm/` - Helm charts (optional)

## Quick Start

See individual subdirectories for specific deployment instructions.
