apiVersion: v1
kind: Service
metadata:
  name: llm-service
  labels:
    app: llm-service
    component: code-generation
spec:
  type: ClusterIP
  selector:
    app: llm-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
